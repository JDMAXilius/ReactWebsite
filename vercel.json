{
  "buildCommand": "cd nexus-dashboard && npm install && npm run build",
  "outputDirectory": "nexus-dashboard/dist",
  "installCommand": "echo 'install handled in buildCommand'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
